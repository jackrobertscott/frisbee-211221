{
  "private": true,
  "name": "core",
  "version": "0.0.0",
  "author": "Jack Scott",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist/*",
    "prepublishOnly": "npm run clean && npm run build",
    "dev": "tsc --watch",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "^20.6.3",
    "jest": "^29.7.0",
    "typescript": "^5.2.2"
  },
  "peerDependencies": {
    "zod": "^3.22.2"
  },
  "dependencies": {
    "torva": "^0.4.0"
  }
}
