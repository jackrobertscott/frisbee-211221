import {css} from '@emotion/css'
import {createElement as $, FC} from 'react'
/**
 *
 */
export const SecuritySignUp: FC = () => {
  return $('div', {
    children: 'SecuritySignUp',
    className: css({
      padding: 13,
    }),
  })
}
