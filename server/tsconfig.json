{
  "compilerOptions": {
    // Input
    "lib": ["ESNext"],
    "target": "ESNext",

    // Output
    // "noEmit": true, // We want it to emit
    "sourceMap": true,
    "declaration": true,
    "outDir": "dist",

    // Import/Export
    "module": "ESNext",
    "moduleResolution": "Node",
    "moduleDetection": "force",
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "skipLibCheck": true,

    // Type Checking
    "strict": true,
    "noUnusedLocals": true,
    // "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    // Paths
    "baseUrl": "..",
    "paths": {
      "@server/*": ["server/src/*"],
      "@shared/*": ["shared/src/*"],
      "torva": ["server/node_modules/torva"] // Hack to get torva to work
    }
  },
  "include": ["src", "../shared/src"],
  "exclude": ["node_modules"]
}
